# Lab 02 Escape Hatch

Restore only the completed `hireEmployee()` method.

Do not replace the whole repository file, because that would also reveal later
labs that share `HrEmployeeCommandJdbcRepository.java`.

Source snippet:

- `reference/lab02/backend/hrapp-service/src/main/java/com/company/hr/repository/HrEmployeeCommandJdbcRepository.hireEmployee.java.txt`

Target file:

- `backend/hrapp-service/src/main/java/com/company/hr/repository/HrEmployeeCommandJdbcRepository.java`

Replace only the `hireEmployee()` method body/signature in the target file and
leave the rest of the repository untouched.
