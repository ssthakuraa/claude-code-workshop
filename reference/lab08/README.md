# Lab 08 Escape Hatch

Restore only the completed `promoteEmployee()` method.

Do not replace the whole repository file, because that would overwrite any
earlier learner work in `hireEmployee()` or other lifecycle methods.

Source snippet:

- `reference/lab08/backend/hrapp-service/src/main/java/com/company/hr/repository/HrEmployeeCommandJdbcRepository.promoteEmployee.java.txt`

Target file:

- `backend/hrapp-service/src/main/java/com/company/hr/repository/HrEmployeeCommandJdbcRepository.java`

Replace only the `promoteEmployee()` method body/signature in the target file
and leave the rest of the repository untouched.
